# D3 指令维度 — 实现索引

> 更新：2026-09-01 ｜ 维护规则：代码/配置变动影响观测点时更新本文档；file:line 随代码漂移需人工同步。

---

## 1. 观测点明细

| ID | 实现位置 | 获取方式 | 状态 |
|---|---|---|---|
| TOP-01 | bag + `ros2 topic hz --window 600`（每项 ≥120s） | bag info 条数对账（方案 §4.1） | ✅ |
| TOP-02 | /joint_states stamp：`dcu_driver_module.cc:401-403`（gettimeofday）+ bag 接收时间 | 离线计算（方案 §4.2） | ✅ |
| TOP-03 | bag 中 /cmd_vel、/cmd_vel_limiter 的 stamp 差分 | 离线计算（方案 §4.1） | ✅ |
| TOP-04 | /joint_cmd 无 stamp（`control_module.cc:234-261`，proto 无 header 字段） | 改造 R2 前用近似法（方案 TC-02） | 🚧 |
| DATA-05 | rosbag（D0 §2.2 ③ 采集命令） | `$RUN/bag/` | ✅ |

## 2. 链路实现位置（TC 判定依据）

| 环节 | 位置 | 当前值 |
|---|---|---|
| Nav2 controller_frequency | `navigation/planning/humanoid_sim/config/nav2_real.yaml:47` | 10.0 |
| MPPI batch_size / time_steps | 同文件 :78/:80 | 600 / 32（D1 降级路径可降至 300/24） |
| local/global costmap 频率 | 同文件 :167/:229 | 5.0 / 1.0 |
| MPPI 速度上限 vx_max / wz_max | 同文件 | 0.4 / 0.4 |
| ControlModule 订阅 | `motion_control/module/control_module/cfg/rl_x1.yaml:4` | `sub_joy_vel_name: /cmd_vel_limiter` |
| /joint_cmd 发布 | `control_module.cc:242-261`（MainLoop 1kHz） | — |

## 3. 本维度缺口与改造（总表见 D0 索引 §2）

| # | 改动 | 补齐 |
|---|---|---|
| G2/R2 | `control_module.cc` MainLoop 给 /joint_cmd 增加发布时刻（proto 无 header 字段则加独立 debug 话题） | TOP-04 精确化 |

R2 实施时统一双时间戳（steady+wall，见 D0 索引 §3），且须先于 bag_analyze.py 落地。

## 4. 工具

bag_analyze.py（未实现，P1-1）覆盖本维度 §4.1/§4.2/TC-01/02/05 指标；落地前用 `ros2 bag info` + plotjuggler 手动分析（D0 索引 §4）。
